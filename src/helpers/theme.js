// import React, { createContext } from "react";
import { createTheme } from "@mui/material/styles";

// const ColorModeContext = createContext({ toggelColorMode: () => {} });

const theme = createTheme({
  palette: {},
  typography: {},
  breakpoints: {},
});

export default theme;
